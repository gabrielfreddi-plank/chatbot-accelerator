'use client'

import type { ReactNode } from 'react'

interface ConditionalFieldProps {
  children?: ReactNode
}

export function GeneratedConditionalField({ children }: ConditionalFieldProps) {
  return <>{children}</>
}
